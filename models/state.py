#!/usr/bin/python3
"""It's a Defines the State class."""
from models.base_model import BaseModel


class State(BaseModel):
    """It's a Represent a state.

    Attributes:
        name (str): It's a The name of the state.
    """

    name = ""
